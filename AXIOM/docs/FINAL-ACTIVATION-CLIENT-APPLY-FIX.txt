Final activation client-apply fix

What was fixed:
- Browser applyClientAction now handles activate_plugin, deactivate_plugin, and plugin_runtime_status.
- applyClientAction is now async and awaited by SSEBridge.call().
- AXIOM_PLUGIN_RUNTIME is actually injected into axiom-editor.html, not just left in snippets.
- SceneManager exposes getCamera(), getOrbitTarget(), and getRendererDomElement().
- window.EDITOR.scene exposes focusSelected(), getCamera(), getOrbitTarget(), and getRendererDomElement().

Expected test:
1. Start Plugin Builder:
   cd C:\Users\felix\Desktop\Automated_AI_Pipeline\Projects\AXIOM\pluginbuilder_finished
   npm run start:http

2. Launch AXIOM normally.

3. Ask AXIOM chat:
   Use axiom_plugin_runtime_status.

Expected result:
- The receipt should now contain clientApplyReceipt.
- Stream should show mcp_client_apply.
- It should no longer stop at pendingClientApply only.

4. Ask:
   Use axiom_plugin_activate.

   plugin_id:
   ViewportNavigationImplementation

Expected result:
- clientApplyReceipt.applied should be true if the plugin source loads successfully.
- If activation fails, it should return a structured activation_failed_rolled_back error instead of pretending success.
