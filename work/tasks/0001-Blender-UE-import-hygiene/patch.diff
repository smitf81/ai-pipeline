```diff
diff --git a/ue_python/bridge_watcher.py b/ue_python/bridge_watcher.py
index 4d1e7a2..05f83de 100644
--- a/ue_python/bridge_watcher.py
+++ b/ue_python/bridge_watcher.py
@@ -1,6 +1,7 @@
 import argparse
 import json
 import logging
+import os
 import time
 from pathlib import Path

 try:  # pragma: no cover - Unreal not available during tests
@@ -49,6 +50,8 @@ class BridgeWatcher:
         except Exception as e:  # pragma: no cover
             logger.error("Cannot create folder: %s", e)
             return

+    def _save_state(self):
+        with open(STATE_FILE, 'w') as f:
+            json.dump(self._state, f)
+
     def start(self):
         if self._handle is not None:
             logger.warning("Already running.")
@@ -68,6 +71,7 @@ class BridgeWatcher:
         _state["imported"].add(str(full))
         logger.info("Imported & spawned: %s", name)

+    def stop(self):
+        self._save_state()
         if self._handle is not None:
             unreal.unregister_slate_post_tick_callback(self._handle)
             self._handle = None
@@ -91,3 +95,20 @@ def configure(args):
     BRIDGE_DIR = Path(args.bridge)
     DEST_CONTENT_PATH = args.content
     LOG_FILE = Path(args.log)
+    STATE_FILE = Path.home() / "bw_state.json"

 def main():
     if unreal is None:
         raise RuntimeError("Unreal Python API not available")
+    state = {}
     args = parse_args()
     configure(args)
+    if STATE_FILE.exists():
+        with open(STATE_FILE, 'r') as f:
+            state = json.load(f)
+
+    bw = BridgeWatcher()
+    bw._state = state
     start_watching(bw)
     try:
         while True:
@@ -98,3 +119,5 @@ def main():
             time.sleep(1)
     except KeyboardInterrupt:
         stop_watching()
+    bw.stop()
```
