# Legacy Archive

This directory holds archived repo content kept for compatibility and reference.

- `legacy/ai-pipeline/` is the previously nested ACE pipeline tree.
- `legacy/Old/` is an untracked local archive of older launcher/UI files.

Active runtime code should not read from this directory.
